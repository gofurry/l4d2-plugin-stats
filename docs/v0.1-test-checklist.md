# v0.1.0 验收清单

## 构建

- [ ] `scripts/validate-migrations.ps1` 三种驱动全部通过。
- [ ] `python scripts/test_sqlite_migration.py` 通过内存 SQLite 集成测试。
- [ ] `scripts/build.ps1` 无 error 和 warning。
- [ ] `dist/l4d2_player_stats.smx` 已生成。
- [ ] `scripts/package.ps1` 的压缩包同时包含 `.smx` 和三套迁移。

## SQLite 运行

- [ ] 未配置唯一 `server_key` 时状态为 `configuration-error`。
- [ ] 配置后首次加载自动完成 0001 迁移。
- [ ] `sm_lps_status` 显示 `ready`、`sqlite` 和 `schema=1/1`。
- [ ] `lps_servers` 有一条当前服务器记录。
- [ ] `lps_server_boots` 有一条 `active` 启动记录。
- [ ] `sm_lps_flush` 更新服务器和启动实例心跳。
- [ ] 重载插件后旧 `active` boot 被标记为 `abandoned`，新 boot 为 `active`。

## 故障边界

- [ ] 数据库不可用时不踢玩家、不写无限本地队列。
- [ ] 重连退避受 `sm_lps_retry_maximum` 限制。
- [ ] 相同错误受日志抑制窗口限制。
- [ ] v0.1 不创建玩家、Session、Run、Round 或 Segment 数据。

## 后续驱动

- [ ] 在独立测试库验证 MySQL 迁移。
- [ ] 在独立测试库验证 PostgreSQL 迁移。
- [ ] 验证共享数据库中不同 `server_key` 不互相覆盖。
