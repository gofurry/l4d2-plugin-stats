# Dashboard (future)

The future Go backend and embedded frontend will live here. It is intentionally
not scaffolded in v0.1: the SourceMod collector and database contracts must be
proven first. The Go side will consume the collector schema as read-only data
and will not own a separate migration history.

